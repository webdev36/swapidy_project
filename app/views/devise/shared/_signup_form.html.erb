<div id="signup_container">
  <img id="signup_circle" src="/images/login_rectangle.png" >
  <% if show_cancel_link %>
    <a href="javascript:hidePopup();" style="color:#666666; font-family:'myriad-s'; font-size:30px; position:relative; right:30px; bottom:230px;">x</a>
  <% end %>
  <a href="<%= user_omniauth_authorize_path(:facebook)%>"><img id="fb_signup" src="/images/fb_signup.png"></a>
  <p class="login_or">
    <span>or</span>
  </p>
  <div id="login_form">
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource)) do |f| %>
      <div class="input-prepend">
        <span class="add-on">E-mail</span>
        <%= f.email_field :email, :class => "span2", :placeholder => "E-mail address", :id => "prependedInput" %>
      </div>
      <div class="input-prepend">
        <span class="add-on">Password</span>
        <%= f.password_field :password, :class => "span2", :id => "prependedInput"  %>
      </div>
      <div class="input-prepend">
        <span class="add-on">PassConfirm</span>
        <%= f.password_field :password_confirmation, :class => "span2", :id => "prependedInput"  %>
      </div>
      <%= f.submit "Sign up", :class => "login_button", :style => "top: 135px;" %>
      <% unless resource.errors.empty? %>
        <span style="color: red;"> !!! <%= raw resource.errors.full_messages.first %>. </span>
      <% end %>
    <% end %>

  </div>
     <div class="sub_footer">By signing up, you agree to our <a href="#">Terms of Service</a> and have read our <a href="#">Privacy Policy</a>.</div>
</div>
