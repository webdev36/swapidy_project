<% 
  if for_item == :orders
    orders = current_user.orders.to_buy
    page_title = "Here's a list of everything you've bought on Swapidy..."
  elsif for_item == :trade_ins
    orders = current_user.orders.to_sell
    page_title = "Here's a list of everything you've sold to Swapidy..."
  else
    orders = current_user.orders
    page_title = "Here's a list of all your transactions..."
  end  
%>

<% if orders.empty? %>
  <label>You don't have any orders</label>
<% else %>
  <label><%= page_title %></label><br/>
  <table class="table">
    <thead>
      <tr>
        <th>No.</th>
        <% if for_item == :all_trans %>
          <th>Sell/Buy</th>
        <% end %>
        <th>Product</th>
        <th>&nbsp;&nbsp;&nbsp;<img src="/images/honey_20px.png"></th>
        <th>Status</th>
        <th>Created At</th>
      </tr>
    </thead>
    <tbody>
      <% orders.each_with_index do |order, index| %>
        <tr>
          <td><%= index + 1 %></td>
          <% if for_item == :all_trans %>
            <td><%= order.is_trade_ins? ? "Sell" : "Buy" %></td>
          <% end %>
          <td><%= order.title %></td>
          <td><%= order.honey_price %></td>
          <td><%= order.status_title %></td>
          <td><%= order.created_at.strftime("%H:%M %b %d, %Y") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
